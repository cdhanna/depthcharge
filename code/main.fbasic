

`TODO: setup basic game structures

` render width and height
global R_WIDTH = 800
global R_HEIGHT = 800
set render size R_WIDTH, R_HEIGHT


set background color rgb(85, 85, 85)
sync

` TODO: run various setup routines
GOSUB _setupWorld

` TODO: render a main menu

GOSUB _setupPlayer
GOSUB _setupBombs
GOSUB _setupFish

time = timer()
do
  sync

  oldTime = time
  time = timer()
  deltaTime = (time - oldTime) / 1000.0

  GOSUB _loopPlayer
  GOSUB _loopBombs


loop
